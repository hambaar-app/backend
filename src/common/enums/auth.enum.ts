export enum AuthRoles {
  Sender = 'sender',
  Transporter = 'transporter'
}

export enum Roles {
  Admin = 'admin',
  Sender = 'sender',
  Transporter = 'transporter'
}

export enum AuthTokens {
  Access = 'access',
  Temporary = 'temp',
  Progress = 'progress'
}